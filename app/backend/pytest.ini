[pytest]
asyncio_mode = auto
testpaths = tests
# Repo root is on the path so ``qe_toolkit`` (the shared QE framework
# extracted at the repo root) is importable from any customer service.
pythonpath = . ../..
filterwarnings =
    ignore::DeprecationWarning
